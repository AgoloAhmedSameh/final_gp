 START ----->  std::pair<int, int> ----->  find_Sum (std::vector<int> &arr, int n) ----->  std::sort (arr.begin (), arr.end ()); ----->  int sum = arr[0]; -----> for (int i = 0; i < n - 1; ++i) ----->  if (arr[i] != arr[i + 1]) ----->  sum += arr[i + 1]; ----->  return { sum, 0 }; -----> END
 START ----->  std::pair<int, int> ----->  find_Sum (std::vector<int> &arr, int n) ----->  std::sort (arr.begin (), arr.end ()); ----->  int sum = arr[0]; -----> for (int i = 0; i < n - 1; ++i) ----->  if (arr[i] != arr[i + 1]) ----->  return { sum, 0 }; -----> END
