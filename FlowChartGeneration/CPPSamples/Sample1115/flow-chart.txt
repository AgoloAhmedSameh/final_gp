 START ----->  std::string ----->  file_name_check (const std::string &file_name) ----->  std::vector<std::string> suf = { "txt", "exe", "dll" }; ----->  size_t pos = file_name.find ('.'); ----->  if (pos == std::string::npos || pos == 0 || pos == file_name.length () - 1) ----->  return "No"; -----> END
 START ----->  std::string ----->  file_name_check (const std::string &file_name) ----->  std::vector<std::string> suf = { "txt", "exe", "dll" }; ----->  size_t pos = file_name.find ('.'); ----->  if (pos == std::string::npos || pos == 0 || pos == file_name.length () - 1) ----->  std::string name = file_name.substr (0, pos); ----->  std::string extension = file_name.substr (pos + 1); ----->  if (std::find (suf.begin (), suf.end (), extension) == suf.end ()) ----->  return "No"; -----> END
 START ----->  std::string ----->  file_name_check (const std::string &file_name) ----->  std::vector<std::string> suf = { "txt", "exe", "dll" }; ----->  size_t pos = file_name.find ('.'); ----->  if (pos == std::string::npos || pos == 0 || pos == file_name.length () - 1) ----->  std::string name = file_name.substr (0, pos); ----->  std::string extension = file_name.substr (pos + 1); ----->  if (std::find (suf.begin (), suf.end (), extension) == suf.end ()) ----->  if (!std::isalpha (name[0])) ----->  return "No"; -----> END
 START ----->  std::string ----->  file_name_check (const std::string &file_name) ----->  std::vector<std::string> suf = { "txt", "exe", "dll" }; ----->  size_t pos = file_name.find ('.'); ----->  if (pos == std::string::npos || pos == 0 || pos == file_name.length () - 1) ----->  std::string name = file_name.substr (0, pos); ----->  std::string extension = file_name.substr (pos + 1); ----->  if (std::find (suf.begin (), suf.end (), extension) == suf.end ()) ----->  if (!std::isalpha (name[0])) ----->  int digit_count = 0; -----> for (char c : name) ----->  if (std::isdigit (c)) ----->  digit_count++; ----->  if (digit_count > 3) ----->  return "No"; -----> END
 START ----->  std::string ----->  file_name_check (const std::string &file_name) ----->  std::vector<std::string> suf = { "txt", "exe", "dll" }; ----->  size_t pos = file_name.find ('.'); ----->  if (pos == std::string::npos || pos == 0 || pos == file_name.length () - 1) ----->  std::string name = file_name.substr (0, pos); ----->  std::string extension = file_name.substr (pos + 1); ----->  if (std::find (suf.begin (), suf.end (), extension) == suf.end ()) ----->  if (!std::isalpha (name[0])) ----->  int digit_count = 0; -----> for (char c : name) ----->  if (std::isdigit (c)) ----->  digit_count++; ----->  if (digit_count > 3) ----->  return "Yes"; -----> END
 START ----->  std::string ----->  file_name_check (const std::string &file_name) ----->  std::vector<std::string> suf = { "txt", "exe", "dll" }; ----->  size_t pos = file_name.find ('.'); ----->  if (pos == std::string::npos || pos == 0 || pos == file_name.length () - 1) ----->  std::string name = file_name.substr (0, pos); ----->  std::string extension = file_name.substr (pos + 1); ----->  if (std::find (suf.begin (), suf.end (), extension) == suf.end ()) ----->  if (!std::isalpha (name[0])) ----->  int digit_count = 0; -----> for (char c : name) ----->  if (std::isdigit (c)) ----->  if (digit_count > 3) ----->  return "No"; -----> END
 START ----->  std::string ----->  file_name_check (const std::string &file_name) ----->  std::vector<std::string> suf = { "txt", "exe", "dll" }; ----->  size_t pos = file_name.find ('.'); ----->  if (pos == std::string::npos || pos == 0 || pos == file_name.length () - 1) ----->  std::string name = file_name.substr (0, pos); ----->  std::string extension = file_name.substr (pos + 1); ----->  if (std::find (suf.begin (), suf.end (), extension) == suf.end ()) ----->  if (!std::isalpha (name[0])) ----->  int digit_count = 0; -----> for (char c : name) ----->  if (std::isdigit (c)) ----->  if (digit_count > 3) ----->  return "Yes"; -----> END
