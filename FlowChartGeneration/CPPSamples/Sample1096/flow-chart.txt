 START ----->  int ----->  add_elements (const std::vector<int> &arr, int k) ----->  int sum = 0; -----> for (int i = 0; i < k && i < arr.size (); ++i) ----->  if (std::to_string (arr[i]).length () <= 2) ----->  sum += arr[i]; ----->  return sum; -----> END
 START ----->  int ----->  add_elements (const std::vector<int> &arr, int k) ----->  int sum = 0; -----> for (int i = 0; i < k && i < arr.size (); ++i) ----->  if (std::to_string (arr[i]).length () <= 2) ----->  return sum; -----> END
