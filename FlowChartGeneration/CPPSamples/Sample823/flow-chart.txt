 START ----->  std::pair<std::vector<int>, std::vector<int> > ----->  remove_even (std::vector<int> &l) -----> for (auto it = l.begin (); it != l.end ();) ----->  if (*it % 2 == 0) ----->  it = l.erase (it); ----->  return { l }; -----> END
 START ----->  std::pair<std::vector<int>, std::vector<int> > ----->  remove_even (std::vector<int> &l) -----> for (auto it = l.begin (); it != l.end ();) ----->  if (*it % 2 == 0) ----->  else ----->  ++it; ----->  return { l }; -----> END
