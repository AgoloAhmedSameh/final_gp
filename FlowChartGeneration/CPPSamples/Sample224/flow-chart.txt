 START ----->  int ----->  find_Min (vector<int> &arr, int low, int high) -----> while (low < high) ----->  int mid = low + (high - low) / 2; ----->  if (arr[mid] == arr[high]) ----->  high -= 1; ----->  return arr[high]; -----> END
 START ----->  int ----->  find_Min (vector<int> &arr, int low, int high) -----> while (low < high) ----->  int mid = low + (high - low) / 2; ----->  if (arr[mid] == arr[high]) ----->  else if (arr[mid] > arr[high]) ----->  low = mid + 1; ----->  return arr[high]; -----> END
 START ----->  int ----->  find_Min (vector<int> &arr, int low, int high) -----> while (low < high) ----->  int mid = low + (high - low) / 2; ----->  if (arr[mid] == arr[high]) ----->  else if (arr[mid] > arr[high]) ----->  else ----->  high = mid; ----->  return arr[high]; -----> END
