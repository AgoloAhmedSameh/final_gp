 START ----->  std::pair<std::string, std::vector<std::tuple<> > > ----->  trim_tuple (const std::vector<std::tuple<> > &test_list, int K) ----->  std::vector<std::tuple<> > res; -----> for (const auto &ele : test_list) ----->  int N = std::tuple_size<decltype (ele)>::value; ----->  std::tuple<> trimmed_tuple; -----> for (int i = K; i < N - K; ++i) ----->  trimmed_tuple = std::tuple_cat (trimmed_tuple, std::make_tuple (std::get<i> (ele))); ----->  res.push_back (trimmed_tuple); ----->  return { std::to_string (res.size ()), res }; -----> END
