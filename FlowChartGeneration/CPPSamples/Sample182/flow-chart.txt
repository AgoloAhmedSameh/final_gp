 START ----->  using namespace std; ----->  pair<int, int> ----->  count_pairs (vector<int> &arr, int n, int k) ----->  int count = 0; -----> for (int i = 0; i < n; i++) -----> for (int j = i + 1; j < n; j++) ----->  if (arr[i] - arr[j] == k || arr[j] - arr[i] == k) ----->  count++;
 START ----->  using namespace std; ----->  pair<int, int> ----->  count_pairs (vector<int> &arr, int n, int k) ----->  int count = 0; -----> for (int i = 0; i < n; i++) -----> for (int j = i + 1; j < n; j++) ----->  if (arr[i] - arr[j] == k || arr[j] - arr[i] == k) ----->  for (int j = i + 1; j < n; j++) ----->  if (arr[i] - arr[j] == k || arr[j] - arr[i] == k) ----->  count++;
 START ----->  using namespace std; ----->  pair<int, int> ----->  count_pairs (vector<int> &arr, int n, int k) ----->  int count = 0; -----> for (int i = 0; i < n; i++) -----> for (int j = i + 1; j < n; j++) ----->  if (arr[i] - arr[j] == k || arr[j] - arr[i] == k) ----->  for (int j = i + 1; j < n; j++) ----->  return make_pair (count, 0); -----> END
