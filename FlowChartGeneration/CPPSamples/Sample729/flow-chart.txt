 START ----->  std::vector<int> ----->  consecutive_duplicates (const std::vector<int> &nums) ----->  std::vector<int> result; ----->  if (nums.empty ()) ----->  return result; -----> END
 START ----->  std::vector<int> ----->  consecutive_duplicates (const std::vector<int> &nums) ----->  std::vector<int> result; ----->  if (nums.empty ()) ----->  result.push_back (nums[0]); -----> for (size_t i = 1; i < nums.size (); ++i) ----->  if (nums[i] != nums[i - 1]) ----->  result.push_back (nums[i]); ----->  return result; -----> END
 START ----->  std::vector<int> ----->  consecutive_duplicates (const std::vector<int> &nums) ----->  std::vector<int> result; ----->  if (nums.empty ()) ----->  result.push_back (nums[0]); -----> for (size_t i = 1; i < nums.size (); ++i) ----->  if (nums[i] != nums[i - 1]) ----->  return result; -----> END
