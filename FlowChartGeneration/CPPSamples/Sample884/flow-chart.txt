 START ----->  bool ----->  is_Isomorphic (const std::string &str1, const std::string &str2) ----->  std::unordered_map<char, std::vector<int> > dict_str1; ----->  std::unordered_map<char, std::vector<int> > dict_str2; -----> for (size_t i = 0; i < str1.size (); ++i) ----->  dict_str1[str1[i]].push_back (i); -----> for (size_t j = 0; j < str2.size (); ++j) ----->  dict_str2[str2[j]].push_back (j); ----->  std::vector<std::vector<int> > values1, values2; -----> for (const auto &pair : dict_str1) ----->  values1.push_back (pair.second); -----> for (const auto &pair : dict_str2) ----->  values2.push_back (pair.second); ----->  std::sort (values1.begin (), values1.end ()); ----->  std::sort (values2.begin (), values2.end ()); ----->  return values1 == values2; -----> END
