 START ----->  std::vector<int> ----->  counting_sort (std::vector<int> &my_list) ----->  int max_value = 0; -----> for (int i = 0; i < my_list.size (); i++) ----->  if (my_list[i] > max_value) ----->  max_value = my_list[i]; ----->  std::vector<int> buckets (max_value + 1, 0); -----> for (int i : my_list) ----->  buckets[i]++; ----->  int i = 0; -----> for (int j = 0; j <= max_value; j++) -----> for (int a = 0; a < buckets[j]; a++) ----->  my_list[i] = j; ----->  i++;
 START ----->  std::vector<int> ----->  counting_sort (std::vector<int> &my_list) ----->  int max_value = 0; -----> for (int i = 0; i < my_list.size (); i++) ----->  if (my_list[i] > max_value) ----->  std::vector<int> buckets (max_value + 1, 0); -----> for (int i : my_list) ----->  buckets[i]++; ----->  int i = 0; -----> for (int j = 0; j <= max_value; j++) -----> for (int a = 0; a < buckets[j]; a++) ----->  my_list[i] = j; ----->  i++;
