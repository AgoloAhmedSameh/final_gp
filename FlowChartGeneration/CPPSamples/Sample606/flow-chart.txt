 START ----->  std::tuple<std::string, int, int> ----->  find_literals (const std::string &text, const std::string &pattern) ----->  std::regex re (pattern); ----->  std::smatch match; ----->  if (std::regex_search (text, match, re)) ----->  int s = match.position (); ----->  int e = s + match.length (); ----->  return std::make_tuple (match.str (), s, e); -----> END
 START ----->  std::tuple<std::string, int, int> ----->  find_literals (const std::string &text, const std::string &pattern) ----->  std::regex re (pattern); ----->  std::smatch match; ----->  if (std::regex_search (text, match, re)) ----->  return std::make_tuple ("", -1, -1); -----> END
