 START ----->  std::pair<int, int> ----->  find_remainder (const std::vector<int> &arr, int lens, int n) ----->  int mul = 1; -----> for (int i = 0; i < lens; ++i) ----->  mul = (mul * (arr[i] % n)) % n; ----->  return { mul % n, 0 }; -----> END
