 START ----->  std::pair<int, int> ----->  even_bit_set_number (int n) ----->  int count = 0, res = 0, temp = n; -----> while (temp > 0) ----->  if (count % 2 == 1) ----->  res |= (1 << count); ----->  count++; ----->  temp >>= 1; ----->  return { n | res, res }; -----> END
 START ----->  std::pair<int, int> ----->  even_bit_set_number (int n) ----->  int count = 0, res = 0, temp = n; -----> while (temp > 0) ----->  if (count % 2 == 1) ----->  count++; ----->  temp >>= 1; ----->  return { n | res, res }; -----> END
