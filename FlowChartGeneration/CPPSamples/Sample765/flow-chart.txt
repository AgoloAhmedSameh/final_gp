 START ----->  std::pair<std::vector<std::pair<int, int> >, std::vector<std::pair<int, int> > > ----->  pair_wise (const std::vector<int> &l1) ----->  std::vector<std::pair<int, int> > temp; -----> for (size_t i = 0; i < l1.size () - 1; ++i) ----->  int current_element = l1[i]; ----->  int next_element = l1[i + 1]; ----->  temp.emplace_back (current_element, next_element); ----->  return { temp }; -----> END
